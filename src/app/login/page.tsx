import { LoginForm } from '@/components/auth/LoginForm';

export default function LoginPage() {
  return (
    <section className="page page--narrow animate__animated animate__fadeIn">
      <LoginForm />
    </section>
  );
}
